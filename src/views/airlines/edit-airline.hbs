<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Edit Airlines</h3>
        </div>
        <div class="card-body">
            <form action="/airlines/edit-airlines/{{airline._id}}?_method=PUT" method="POST">
            <input type="hidden" name="_method" value="PUT">
            <div class="form-group">
                Airport ID:
                    <input type="text" name="airportId" value="{{airline.id}}""></br>
                Name:
                    <input type="text" name="airlineName" value="{{airline.name}}""></br>

                Select Country: 
                    <select id ="country" name ="country" >{{airport.name}}</select></br>
            </div>
            <div class="form-group">
                <button class="bton btn-primary btn-block" type="submit">
                    Save
                </button>
            </div>
            </form>
        </div>
    </div>
</div>